namespace Silicus.Finder.Entities
{
    public enum ConnectionType
    {
        User, // Database that stores membership user data
        Ip, // database that stores intellectual property data for this application.
        Common //database that stores common data
    }
}